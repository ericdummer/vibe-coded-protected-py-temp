def pytest_configure():
    pass

@pytest.fixture
def sample_fixture():
    return "sample data"

def pytest_addoption(parser):
    parser.addoption("--option", action="store", default="default_value", help="An example option")

def pytest_collection_modifyitems(config, items):
    if config.getoption("--option") == "skip":
        skip_marker = pytest.mark.skip(reason="Skipping tests due to option")
        for item in items:
            item.add_marker(skip_marker)

import requests

def test_api_endpoint():
    response = requests.get("http://localhost:5000/api/endpoint")
    assert response.status_code == 200
    assert response.json() == {"message": "success"}

def test_api_endpoint_error():
    response = requests.get("http://localhost:5000/api/invalid")
    assert response.status_code == 404

def test_config_value():
    assert config_value == "expected_value"

def test_config_setting():
    assert config_setting == True

def test_hello_world():
    assert hello_world() == "Hello, World!"

def test_hello_world_edge_case():
    assert hello_world("") == "Hello, World!"